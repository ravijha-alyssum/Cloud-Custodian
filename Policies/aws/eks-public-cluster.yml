policies:
  - name: eks-public-cluster-endpoints
    resource: aws.eks-cluster
    comment: |
      This policy identifies EKS clusters with publicly accessible API server endpoints.
    filters:
      - resourcesVpcConfig.endpointPublicAccess: true